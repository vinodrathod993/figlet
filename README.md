# This is my first project using server side programming 
# In these project i used the node.js.
In the First step I installed the figlet  by using the git bash in  our current folder of our project as npm install figlet.
# Second Step
I noticed that after installation there are three folders node_modules ,package-lock.json and package.json 
# Index.js
Finally Written the javascript code which is going to require the figlet content and running the code in the terminal 
